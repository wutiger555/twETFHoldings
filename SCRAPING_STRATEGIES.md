# 爬蟲策略說明文檔

本文檔旨在詳細記錄專案中針對每一家證券投信公司的 ETF 爬蟲所採用的具體技術策略。當未來目標網站改版導致爬蟲失效時，本文件將作為快速診斷與修復問題的核心參考。

---

## 1. 元大投信 (Yuanta)

- **`firm` ID**: `yuanta`
- **核心方法**: Selenium WebDriver (動態渲染)
- **`needs_browser`**: `True`

### 策略原因

元大投信的持股比例頁面是一個**動態載入的單頁應用 (SPA)**。直接使用 HTTP 請求只能獲取頁面的基本框架，而持股資料本身是透過 JavaScript 在瀏覽器環境中非同步載入的。此外，完整的持股列表需要使用者點擊「顯示更多」按鈕才能完全展開。因此，必須使用像 Selenium 這樣的工具來模擬真實的瀏覽器行為。

### 抓取步驟詳解

1.  **載入頁面**: 使用 Selenium `driver.get()` 方法訪問目標 URL (`https://www.yuantaetfs.com/product/detail/{etf_code}/ratio`)。
2.  **等待主要內容**: 使用 `WebDriverWait` 等待頁面最外層的容器 (`.mainBox`) 出現，確保頁面主體已載入。
3.  **處理彈出視窗**: 嘗試尋找一個 ID 為 `outsidePop` 的彈出視窗。如果存在，則執行 JavaScript (`arguments[0].style.display='none';`) 將其隱藏，以防干擾後續操作。
4.  **點擊「顯示更多」**: 這是最關鍵的一步。腳本會尋找並點擊 class 為 `.moreBtn` 的按鈕，以展開完整的持股列表。點擊後會等待 2 秒，確保資料有足夠時間載入。
5.  **解析頁面**: 在所有互動完成後，將 `driver.page_source` 交給 `BeautifulSoup` 進行解析。
6.  **抓取 ETF 名稱**: 從頁面的 `<title>` 標籤中解析出 ETF 名稱。
7.  **定位持股表格**: 透過遍歷所有 `div` 元素，找到那個包含了 `<h3>` 標籤且其文字內容包含「基金權重-股票」的 `div`，這個 `div` 就是持股表格的容器。
8.  **遍歷資料**: 選取容器中所有的資料列 (`.table .tbody .tr`)，並逐行解析 `code`, `name`, `shares`, `weight` 等欄位。

---

## 2. 富邦投信 (Fubon)

- **`firm` ID**: `fubon`
- **核心方法**: `requests` (直接 HTTP 請求)
- **`needs_browser`**: `False`

### 策略原因

與元大不同，富邦投信的網站在伺服器端就產生了包含所有持股資料的完整 HTML。雖然頁面上也有 JavaScript，但核心資料並非非同步載入。因此，使用輕量級的 `requests` 函式庫是最高效的方法，可以完全避免啟動瀏覽器的額外開銷。

**特殊情況**: 該網站在進行 HTTPS 連線時存在 SSL 憑證問題，會導致 `requests` 驗證失敗。因此在請求中加入了 `verify=False` 參數作為解決方案，並抑制了相關的警告訊息。

### 抓取步驟詳解

1.  **發送 HTTP 請求**: 使用 `requests.get()` 訪問目標 URL (`https://websys.fsit.com.tw/FubonETF/Fund/Assets.aspx?stkId={etf_code}`) 並獲取 HTML 全文。
2.  **解析頁面**: 將 `response.text` 交給 `BeautifulSoup` 進行解析。
3.  **抓取基金淨資產**: 首先，在頁面中找到 class 為 `.fund_box.p2` 的區塊，從中解析出「基金淨資產(新台幣)」的總額。這個值將用於後續的債券權重計算。
4.  **遍歷資產類別**: 腳本會遍歷一個預設好的字典 `{"股票": "stock", "期貨": "future", ...}`。
5.  **定位各類別表格**: 對於每種資產，腳本會先尋找對應的 `<h6>` 標題 (例如，文字為「股票」的 `<h6>` 標籤)，然後透過 `.find_next_sibling('div').find('table')` 找到緊跟在標題後的資料表格。
6.  **解析與計算**: 
    - 對於「股票」和「期貨」，直接解析表格中的欄位。
    - 對於「附買回債券」，由於缺少權重資料，腳本會利用**步驟 3** 中得到的基金淨資產總額，透過公式 `(債券金額 / 淨資產總額) * 100` 來**手動計算**其權重。
7.  **統一資料格式**: 將所有解析和計算出的資料，全部存入一個列表中，並確保每筆資料都包含 `asset_type` 欄位。
8.  **返回結果**: 將包含 ETF 名稱和所有持股資料的字典返回給主執行緒。

---

## 3. 國泰投信 (Cathay)

- **`firm` ID**: `cathay`
- **狀態**: 待開發
